This module is for detecting capitalisation in article text.

Steps to run the code:

1. Insert configuration details into files/configure.yaml
2. Run detect_capitalisation.py

    ```shell
    python3 detect_capitalisation.py
    ```
